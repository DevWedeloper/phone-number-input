# @phone-number-input/core

Shared core logic for the Phone Number Input project.

This package contains framework-agnostic utilities and business logic used by the framework-specific implementations. It is not intended to be consumed directly by end users.
